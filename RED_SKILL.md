# Red Skill 对外文案（发布用）

> 用途：小红书 Red Skill 上传表单的填表文案，以及任何对外平台（公众号 / 即刻 / 朋友圈）的介绍素材。
> 仓库里的 README.md 是 GitHub 技术文档视角，SKILL.md 是给 agent 读的协议，**都不是给人看的对外文案**——对外一律用本文件。
> 最后更新：2026-09-02（按用户实际操作方式修正了「怎么用」段）

---

## 一、表单速填

| 字段 | 值 |
|------|-----|
| **Skill 名称** | 多agent异步协同工作流（13/15） |
| **Skill ID** | `multi-agent-async-workflow` |
| **版本号** | `1.0.0` |
| **场景标签** | 效率工具 / AI编程 / 开发工具（以平台实际下拉选项为准，≥1） |

### 名称候选（换平台时可选）

- 多agent异步协同工作流 — 现用，稳、专业
- 让几个AI一起干活 — 大白话，钩子强，适合面向非技术人群
- 让AI自己排队干活 — 画面感强
- AI流水线搭起来 — 最短

---

## 二、卡片简介（展示在 skill 卡片上，60~80 字）

把 GitHub Issues 当任务总线，几个 AI 各开一个窗口排队干活。谁干完谁换标签，下游自己看得见，不用人去通知。你不用挨个盯 PR，只看队列健康度就行。

---

## 三、Skill 介绍（详情页全文）

这是一套协议，不是一条 prompt。

几个 AI 各开一个 session，围着同一个仓库的 Issue 队列转。没有中心调度器，也不需要共享上下文——每个节点只循环做一件事：看队列、认领自己能处理的、干完把标签换掉、再去看队列。状态本身就是通知。

为什么不开一个共享上下文的 agent team？因为审核的价值来自独立性。继承了实施者全部上下文的 reviewer，已经被对方的推理链说服了，抓不到错。这套协议里 Reviewer 只看 PR 和它反查到的 Issue——空上下文不是缺陷，是它唯一的资产。

三条硬规则：状态即通知（只干活不换标签 = 任务静默死掉）；不能审自己写的代码（唯一的红线）；上下文靠工件传递（Issue 写 why/what/验收标准，PR 写 how/diff，没写下来的等于不存在）。

你的位置从「review 每个 PR」变成「review 这套系统」：看吞吐、看打回率、看 blocked 在哪堆积。指标不对时，改的是 Issue 模板和节点 prompt。

### 怎么用

先准备一个 GitHub 仓库当总线，里面要有这 9 个标签：backlog / ready / in_progress / needs-review / approved / done / needs-lead / blocked / needs-human。开工前 skill 会自己检查，缺了会告诉你去补。

然后把 skill 甩给 agent：

- **先甩给一个 agent，让它自己读一遍，定它为总负责人。** 它负责拆需求、派活、看着队列别堵。
- **干活的怎么来，两条路都行**：让总负责人自己往下指派，下游节点扫队列认领；或者你再开一个 AI，把 skill 甩给它、告诉它「你是干活的」。
- **每个 agent 都要自己读一遍 skill。** 它们各开各的 session，谁也不知道别人聊过什么，只认 Issue 和 PR 上写下来的东西——这是设计，不是缺陷。正因为空着上下文，review 才抓得到错。

---

## 四、精简版（100 字内，字数受限时用）

用 GitHub Issues 当任务总线，让多个 AI 节点异步协作。每个节点一个独立 session，循环做「扫队列 → 认领 → 处理 → 换标签」。上下文靠 Issue 和 PR 传递，不靠共享会话。硬红线只有一条：不能审自己写的代码。人负责定协议、看队列健康度。

---

## 五、写这段文案时的几条约定

1. **三处术语是 README 原话内核，不要改成更"好看"的词**：「状态即通知」「不能审自己写的代码」「空上下文不是缺陷，是它唯一的资产」。
2. **视角铁律**：对外文案一律从「平台用户装完即用」写，不要掺 `references/setup.md` 的 clone / 配环境视角。受众分三种——开发者（配环境）、平台用户（装完即用）、终端消费者，搞错视角 = 文案作废。
3. **硬前提必须前置**：GitHub 仓库 + 9 个标签。这是跑起来的必要条件，不写清楚用户装完跑不动会懵。
4. **「每个 agent 都要自己读一遍 skill」不能省**：这是独立 session 设计的直接后果，也是最容易被人误解成"麻烦"的地方，讲清楚它反而成了卖点。
5. 贡献者署名（群友zzszmyf）未写入正文，需要时加在结尾。
